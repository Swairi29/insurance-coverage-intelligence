# --- Core services ---
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
requests>=2.31
httpx>=0.27
python-multipart>=0.0.9

# --- LLM ---
google-genai>=2.24,<3
ollama>=0.4

# --- NLP (Agent 1) ---
spacy>=3.7

# --- Documents & retrieval (Agent 2) ---
pymupdf>=1.24
scikit-learn>=1.4
rank-bm25>=0.2
chromadb>=0.5
pytesseract>=0.3

# --- Security ---
pyjwt>=2.8
# passlib is unmaintained and breaks with bcrypt>=4.1, so bcrypt is used directly.
bcrypt>=4.1
cryptography>=42.0

# --- Frontend ---
streamlit>=1.35

# --- Testing & evaluation ---
pytest>=8.0
pandas>=2.2
matplotlib>=3.8

# Note: pytesseract also requires installing the Tesseract OCR program
# separately (it is not something `pip install` can provide). Page
# rasterization uses PyMuPDF itself, so pdf2image/poppler are not needed.